#include "main.h"
/**
 * add - function that adds two integers
 * @a: first parameter
 * @b: second parameter
 * Return: returns the result of the addition
 */
int add(int a, int b)
{
	return (a + b);
}
