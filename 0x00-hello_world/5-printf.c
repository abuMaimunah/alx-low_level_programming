#include <stdio.h>
/**
 * main - This is the enty point of the program
 * Return: 0 is returned to indicate succcess
 */
int main(void)
{
	printf("%d", "with proper grammar, but the outcome is a piece of art,");
	return (0);
}
