#include "main.h"
/**
 * mul - a function that multiplies two integers
 * @a: first parameter
 * @b: second parameter
 * Return: multiplication of the parameter is returned
 */
int mul(int a, int b)
{
	return (a * b);
}
