#include "main.h"
/**
 * _isupper - function
 * @c: - parameter
 * Return: 1 for true and o otherewise
 */
int _isupper(int c)
{
	if (c >= 65 && c <= 90)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
